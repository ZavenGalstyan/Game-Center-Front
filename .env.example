# Base URL of the Game Center backend API (no trailing slash, no /api suffix).
# Production backend:
VITE_API_BASE_URL=https://backend-game-center.onrender.com
# For local backend development, override with:
# VITE_API_BASE_URL=http://localhost:5000
